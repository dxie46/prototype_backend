{
    "builds": [
        {
            "src": "api/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/eye-pressure/predict",
            "dest": "api/server.py"
        }
    ]
}
